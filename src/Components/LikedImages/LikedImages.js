import React from 'react';
import { Router, Link, NavLink } from 'react-router-dom';

const LikedImages = () => {
    return (
        <div>
            
        </div>
    );
};

export default LikedImages;